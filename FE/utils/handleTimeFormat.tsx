// import moment from 'moment';

// export const handleTimeFormat = (timestamp : string) => {
//     const givenTimestamp = new Date(timestamp);
//     const milisecDiff = Date.now() - moment(givenTimestamp).format('x');
//     const hourDiff = milisecDiff / (1000 * 60 * 60);
//     if (hourDiff > 24) return moment(timestamp).format('MMM Do YY');
//     else return moment(timestamp).fromNow();
// };
